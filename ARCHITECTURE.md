# Headless WooCommerce Architecture Diagram

## 🏗️ System Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    NEXT.JS FRONTEND                          │
│                   (localhost:3001)                           │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌────────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │   Pages    │  │Components│  │  Store   │  │   API    │ │
│  │            │  │          │  │ (Zustand)│  │ Clients  │ │
│  │ - Home     │  │ - Header │  │          │  │          │ │
│  │ - Products │  │ - Cart   │  │ - Cart   │  │ - woo    │ │
│  │ - Cart     │  │ - Product│  │ - Auth   │  │ - store  │ │
│  │ - Checkout │  │ - Auth   │  │          │  │ - auth   │ │
│  │ - Account  │  │          │  │          │  │ - wp     │ │
│  └────────────┘  └──────────┘  └──────────┘  └──────────┘ │
│                                                 ▲            │
└─────────────────────────────────────────────────┼────────────┘
                                                  │
                    ┌─────────────────────────────┼────────────────────┐
                    │                             │                    │
                    ▼                             ▼                    ▼
        ┌───────────────────┐       ┌─────────────────────┐  ┌──────────────────┐
        │  WooCommerce      │       │  WooCommerce        │  │  WordPress       │
        │  REST API         │       │  Store API          │  │  REST API        │
        │  /wc/v3           │       │  /wc/store/v1       │  │  /wp/v2          │
        │                   │       │                     │  │                  │
        │ ✓ Products        │       │ ✓ Cart              │  │ ✓ Pages          │
        │ ✓ Categories      │       │ ✓ Checkout          │  │ ✓ ACF Fields     │
        │ ✓ Reviews         │       │ ✓ Shipping          │  │ ✓ Media          │
        │ ✓ Orders          │       │ ✓ Tax               │  │ ✓ Settings       │
        └───────────────────┘       └─────────────────────┘  └──────────────────┘
                    │                             │                    │
                    └─────────────────────────────┼────────────────────┘
                                                  │
                                                  ▼
                    ┌─────────────────────────────────────────────────┐
                    │         WORDPRESS + WOOCOMMERCE                 │
                    │            (localhost:3000)                     │
                    ├─────────────────────────────────────────────────┤
                    │                                                 │
                    │  ┌──────────────┐  ┌──────────────────────┐   │
                    │  │  WordPress   │  │  WooCommerce         │   │
                    │  │              │  │                      │   │
                    │  │ - CMS        │  │ - Products           │   │
                    │  │ - Users      │  │ - Orders             │   │
                    │  │ - Pages      │  │ - Customers          │   │
                    │  │ - Media      │  │ - Payments           │   │
                    │  │ - Settings   │  │ - Shipping           │   │
                    │  └──────────────┘  └──────────────────────┘   │
                    │                                                 │
                    │  ┌──────────────────────────────────────────┐ │
                    │  │         MySQL Database                   │ │
                    │  └──────────────────────────────────────────┘ │
                    └─────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Examples

### 1. Product Listing Page

```
┌──────────────┐
│   User       │
│  Visits      │
│ /products    │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────┐
│ Next.js Server Component     │
│ products/page.tsx            │
│                              │
│ const products = await       │
│   woocommerce.getProducts()  │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ lib/woocommerce.ts           │
│                              │
│ GET /wc/v3/products          │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ WordPress REST API           │
│                              │
│ Returns product data         │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ Products rendered            │
│ with images, prices, etc.    │
└──────────────────────────────┘
```

### 2. Add to Cart Flow

```
┌──────────────┐
│   User       │
│  Clicks      │
│ "Add to Cart"│
└──────┬───────┘
       │
       ▼
┌──────────────────────────────┐
│ ProductCard.tsx (Client)     │
│                              │
│ onClick={() =>               │
│   storeAPI.addToCart(id)     │
│ }                            │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ lib/store-api.ts             │
│                              │
│ POST /wc/store/v1/cart/      │
│      add-item                │
│                              │
│ {                            │
│   id: productId,             │
│   quantity: 1                │
│ }                            │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ WooCommerce Store API        │
│                              │
│ - Validates product          │
│ - Checks stock               │
│ - Calculates tax             │
│ - Updates session cart       │
│ - Returns updated cart       │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ Cart sidebar updates         │
│ Shows new item count         │
└──────────────────────────────┘
```

### 3. Checkout Flow

```
┌──────────────┐
│   User       │
│  Submits     │
│ Checkout Form│
└──────┬───────┘
       │
       ▼
┌──────────────────────────────┐
│ checkout/page.tsx            │
│                              │
│ await storeAPI.checkout({    │
│   billing_address: {...},    │
│   payment_method: 'cod'      │
│ })                           │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ lib/store-api.ts             │
│                              │
│ POST /wc/store/v1/checkout   │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ WooCommerce Store API        │
│                              │
│ - Validates cart             │
│ - Validates address          │
│ - Processes payment          │
│ - Creates order              │
│ - Sends email                │
│ - Clears cart                │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ Returns order object         │
│                              │
│ {                            │
│   order_id: 123,             │
│   order_key: 'wc_...',       │
│   status: 'processing'       │
│ }                            │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ Redirect to success page     │
│ /order-received/123          │
└──────────────────────────────┘
```

### 4. User Authentication

```
┌──────────────┐
│   User       │
│  Enters      │
│ Credentials  │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────┐
│ LoginForm.tsx                │
│                              │
│ await authAPI.login({        │
│   username: 'john',          │
│   password: 'pass123'        │
│ })                           │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ lib/auth.ts                  │
│                              │
│ POST /jwt-auth/v1/token      │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ JWT Auth Plugin              │
│                              │
│ - Validates credentials      │
│ - Generates JWT token        │
│ - Returns user data          │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ Token stored in localStorage │
│ User redirected to /account  │
└──────────────────────────────┘
```

---

## 📊 API Endpoints Map

### WooCommerce REST API (/wc/v3)
```
GET    /products              → List all products
GET    /products/{id}         → Single product
GET    /products/{id}/reviews → Product reviews
GET    /categories            → Product categories
POST   /orders                → Create order (admin)
GET    /orders                → List orders (admin)
```

### WooCommerce Store API (/wc/store/v1)
```
GET    /cart                  → Get current cart
POST   /cart/add-item         → Add item to cart
POST   /cart/update-item      → Update quantity
POST   /cart/remove-item      → Remove item
POST   /cart/apply-coupon     → Apply discount code
POST   /cart/update-customer  → Update shipping info
POST   /checkout              → Place order
```

### WordPress REST API (/wp/v2)
```
GET    /pages                 → List pages
GET    /pages?slug={slug}     → Get page by slug
GET    /media/{id}            → Get media file
GET    /banners               → Custom post type
```

### JWT Auth API (/jwt-auth/v1)
```
POST   /token                 → Login (get token)
POST   /token/validate        → Validate token
```

---

## 🔐 Security Flow

```
┌─────────────────────────────────────────────────────────┐
│  AUTHENTICATION LAYER                                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Public Routes:                                         │
│  ✓ Products (read-only)                                │
│  ✓ Cart (session-based)                                │
│  ✓ Checkout (guest allowed)                            │
│                                                         │
│  Protected Routes (Require JWT Token):                 │
│  🔒 User Account                                        │
│  🔒 Order History                                       │
│  🔒 Address Management                                  │
│  🔒 Wishlist                                            │
│                                                         │
│  Admin Routes (WooCommerce Credentials):               │
│  🔐 Create/Edit Products                                │
│  🔐 Manage Orders                                       │
│  🔐 Customer Management                                 │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 Performance Optimizations

```
┌────────────────────────────────────────────┐
│  CACHING STRATEGY                          │
├────────────────────────────────────────────┤
│                                            │
│  Next.js Server Components:                │
│  • Products: Cache 1 hour (revalidate)     │
│  • Categories: Cache 24 hours              │
│  • CMS Content: Cache 1 hour               │
│                                            │
│  Client Components:                        │
│  • Cart: Real-time (no cache)              │
│  • User Data: Session storage              │
│  • Auth Token: localStorage                │
│                                            │
│  Production Additions:                     │
│  • Redis for API response caching          │
│  • CDN for images (Cloudflare/CloudFront)  │
│  • Edge caching for static content         │
│                                            │
└────────────────────────────────────────────┘
```

---

## 📱 Deployment Architecture

```
PRODUCTION SETUP:

┌─────────────────┐
│   Vercel/       │ ← Next.js Frontend
│   Netlify       │   (Static + SSR)
└────────┬────────┘
         │
         │ HTTPS
         │
         ▼
┌─────────────────┐
│   WordPress     │ ← Backend API
│   + WooCommerce │   (VPS/Cloud)
│                 │
│   + MySQL       │
│   + Redis       │
└─────────────────┘

BENEFITS:
✓ Frontend: Global CDN, instant scaling
✓ Backend: Full control, custom config
✓ Separation: Update frontend without touching backend
```

---

## 🎯 Summary

This architecture gives you:

✅ **Scalability** - Frontend and backend scale independently  
✅ **Performance** - Static generation + API caching  
✅ **Flexibility** - Full control over UX without WooCommerce theme limitations  
✅ **Reliability** - WooCommerce handles complex e-commerce logic (tax, shipping, payments)  
✅ **Maintainability** - Clear separation of concerns  

**WooCommerce** = Your reliable e-commerce engine  
**Next.js** = Your beautiful, fast storefront
